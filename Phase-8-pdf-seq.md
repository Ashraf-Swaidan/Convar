# Phase 8 — PDF branch (planned)

Separate from image conversion. Per Overview:

- PDF merge / split
- Likely a different library than sharp (e.g. pdf-lib, mutool, or similar)
- Own IPC surface and UI section — not mixed into the image batch pipeline

## Not started

Track as its own phase when image ingest + formats are stable.
